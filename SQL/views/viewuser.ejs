<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VIEW USERS</title>
    <style>
        table,th,td {
            border: solid 1px black;
        }
        td,th{
            padding: 2px 10px;
        }
        a {
            text-decoration: none;
        }
    </style>
</head>
<body>
    <a href="http://localhost:8080/user/new"> Add a New User </a>
    <table>
        <tr>
            <th>ID</th>
            <th>Username</th>
            <th>email</th>
            <th>Password</th>
        </tr>
        <% for (user of result){ %>
            <tr>
                 <td> <%= user.id %> </td>
                 <td> <%= user.username %></td>
                 <td> <%= user.email %></td>
                 <td> <%= user.password %> </td>
                 <form action="/user/<%= user.id %>/edit" method="get">
                    <td><button>Edit</button></td>
                 </form>
                 <form action="/user/<%= user.id %>/delete" method="get">
                    <td><button>Delete</button></td>
                 </form>
            </tr>
         <% } %>
    </table>
</body>
</html>